{
  "name": "redocify",
  "version": "1.0.0",
  "description": "A browserify transform that uses react-docgen to extract information from React component files",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babelify": "^6.3.0",
    "browserify": "^11.0.1",
    "react": "^0.13.3"
  },
  "dependencies": {
    "object-assign": "^4.0.1",
    "react-docgen": "^2.1.0"
  }
}
